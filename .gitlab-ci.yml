stages:
  - init
  - plan
  - apply
  - destroy

image:
  name: hashicorp/terraform:latest
  entrypoint:
    - '/usr/bin/env'
    - 'PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin'


before_script:
  - export AWS_ACCESS_KEY_ID="$AWS_ACCESS_KEY_ID"
  - export AWS_SECRET_ACCESS_KEY="$AWS_SECRET_ACCESS_KEY"
  - export AWS_DEFAULT_REGION="$AWS_DEFAULT_REGION"
  - rm -rf .terraform
  - terraform --version
  - terraform validate

init:
  stage: init
  script:
    - terraform init -backend-config="bucket=$BUCKET_NAME" -backend-config="key=$ENVIRONMENT_NAME/$FILENAME"
  when: manual


plan: 
  stage: plan
  script: 
    - terraform plan -out "planfile" -var-file="variables.tfvars"
  dependencies:
    - validate
  artifacts:
    paths:
      - planfile
  when: manual

apply: 
  stage: apply
  script: 
    - terraform apply --auto-approve -input=false "planfile" -var-file="variables.tfvars"
  dependencies: 
    - plan
  when: manual

destroy:
  stage: destroy
  script:
    - terraform destroy --auto-approve -var-file="variables.tfvars"
  dependencies: 
    - apply 
  when: manual